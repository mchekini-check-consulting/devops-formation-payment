# 


#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Récupérer les arguments du push
z40=0000000000000000000000000000000000000000

while read local_ref local_sha remote_ref remote_sha
do
    if [ "$local_sha" = $z40 ]; then
        # Suppression, on ignore
        continue
    fi
    
    if [ "$remote_sha" = $z40 ]; then
        # Nouvelle branche : vérifier uniquement les commits depuis la branche par défaut
        # Remplacer 'main' par votre branche par défaut (master, develop, etc.)
        range="main..$local_sha"
    else
        # Branche existante : uniquement les nouveaux commits
        range="$remote_sha..$local_sha"
    fi
    
    # Vérifier les commits avec commitlint
    if [ -n "$range" ]; then
        npx commitlint --from "$range" --to "$local_sha"
        if [ $? -ne 0 ]; then
            exit 1
        fi
    fi
done

exit 0